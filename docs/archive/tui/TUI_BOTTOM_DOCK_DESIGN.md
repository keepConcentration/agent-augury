# TUI bottom chrome + terminal-streamed logs (v1.4) — ARCHIVED

> **Superseded by** [`MULTI_FRONT_DESIGN.md`](../../architecture/MULTI_FRONT_DESIGN.md).
> Historical note: `full_screen=False` + `run_in_terminal` bottom chrome; later
> refined in Static bottom-dock notes, then replaced by multi-front Wire plan.

## Approach (historical)

1. Stream logs to host terminal via `run_in_terminal`.
2. Layout = choice? + input + status.
3. `mouse_support=False`, `full_screen=False`.
